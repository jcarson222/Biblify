import axios from "axios";
import { useQuery } from "@tanstack/react-query";

const passageSearchUrl = "https://api.esv.org/v3/passage/text/?q=";

const PassageLookup = () => {
  return <h2>PassageLookup</h2>;
};

export default PassageLookup;
